<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Happy Birthday Jasleen! 🎉</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background: linear-gradient(to bottom right, #ff9a9e, #fad0c4);
      font-family: 'Comic Sans MS', cursive, sans-serif;
      color: #fff;
      text-align: center;
      overflow: hidden;
    }

    h1 {
      font-size: 3em;
      margin-top: 60px;
      animation: bounce 1.5s infinite;
      color: #fff;
      text-shadow: 2px 2px #ff69b4;
    }

    h2 {
      margin-top: 20px;
      font-size: 2em;
      color: #ffe;
      text-shadow: 1px 1px #ff6f91;
    }

    .cake {
      font-size: 80px;
      margin: 20px 0;
    }

    @keyframes bounce {
      0%, 100% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-20px);
      }
    }

    .balloon {
      position: absolute;
      bottom: -150px;
      width: 50px;
      height: 70px;
      background-color: #ff6f91;
      border-radius: 0 0 50% 50%;
      animation: float 6s linear infinite;
    }

    @keyframes float {
      0% {
        bottom: -150px;
        transform: translateX(0);
      }
      100% {
        bottom: 100%;
        transform: translateX(100px);
      }
    }

    .confetti {
      position: fixed;
      width: 10px;
      height: 10px;
      background: yellow;
      top: -10px;
      animation: fall 5s linear infinite;
    }

    @keyframes fall {
      0% {
        top: -10px;
        left: calc(100% * var(--random-x));
      }
      100% {
        top: 100%;
        left: calc(100% * var(--random-x) + 100px);
      }
    }

    audio {
      margin-top: 30px;
    }

    .footer {
      position: fixed;
      bottom: 10px;
      width: 100%;
      font-size: 1.2em;
    }
  </style>
</head>
<body>

  <h1>🎉 Happy Birthday Jasleen! 🎉</h1>
  <h2>Wishing you joy, love, and laughter today and always.</h2>
  <div class="cake">🎂</div>

  <!-- Balloons -->
  <div class="balloon" style="left: 20%; background-color: #ff4d6d;"></div>
  <div class="balloon" style="left: 40%; background-color: #ffc75f;"></div>
  <div class="balloon" style="left: 60%; background-color: #845ec2;"></div>
  <div class="balloon" style="left: 80%; background-color: #00c9a7;"></div>

  <!-- Confetti -->
  <script>
    for (let i = 0; i < 100; i++) {
      const confetti = document.createElement('div');
      confetti.classList.add('confetti');
      confetti.style.left = Math.random() * 100 + 'vw';
      confetti.style.backgroundColor = ['#fce38a', '#f38181', '#95e1d3', '#ff6f91'][Math.floor(Math.random()*4)];
      confetti.style.setProperty('--random-x', Math.random());
      confetti.style.animationDuration = (Math.random() * 3 + 2) + 's';
      document.body.appendChild(confetti);
    }
  </script>

  <!-- Background Music -->
  <audio controls autoplay loop>
    <source src="https://www.bensound.com/bensound-music/bensound-sunny.mp3" type="audio/mpeg">
    Your browser does not support the audio element.
  </audio>

  <div class="footer">With 💖 from your friends & ChatGPT</div>
</body>
</html>
